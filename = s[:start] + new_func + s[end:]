[1mdiff --git a/app.js b/app.js[m
[1mindex 3526d0e..886b809 100644[m
[1m--- a/app.js[m
[1m+++ b/app.js[m
[36m@@ -714,7 +714,38 @@[m [mfunction getCampaignProducts() {[m
   const unique = new Map(managed.map(product => [Number(product.id), product]));[m
   return unique.size ? [...unique.values()] : products;[m
 }[m
[31m-function productMarkup(product) { const image = primaryProductImage(product); return `<article class="product-card"><div class="product-image"><img src="${escapeHtml(image)}" alt="${escapeHtml(product.name)}" loading="lazy"><button class="wishlist" aria-label="Add ${escapeHtml(product.name)} to wishlist">♡</button><span class="discount">${escapeHtml(product.discount || '')}</span></div><div class="product-info"><h3>${escapeHtml(product.name)}</h3><div><span class="stars">★★★★★</span><span class="rating">(${escapeHtml(product.rating || '4.8')})</span></div><div class="price"><strong>${money(product.price)}</strong><span class="old-price">${money(product.old)}</span></div><small class="stock-note">${product.stock > 0 ? `${product.stock}টি স্টকে আছে` : 'স্টক শেষ'}</small><div class="product-actions"><button class="add-button" data-add="${product.id}" ${product.stock < 1 ? 'disabled' : ''}>${product.stock > 0 ? 'ব্যাগে যোগ করুন' : 'স্টক শেষ'}</button><button class="buy-now-button add-button" data-buy-now="${product.id}" ${product.stock < 1 ? 'disabled' : ''}>Buy now</button></div></div></article>`; }[m
[32m+[m[32mfunction productMarkup(product) {[m
[32m+[m[32m  const image = primaryProductImage(product);[m
[32m+[m[32m  return `<article class="product-card">[m
[32m+[m[32m    <div class="product-image">[m
[32m+[m[32m      <img src="${escapeHtml(image)}" alt="${escapeHtml(product.name)}" loading="lazy">[m
[32m+[m[32m      <button class="wishlist" aria-label="Add ${escapeHtml(product.name)} to wishlist">♡</button>[m
[32m+[m[32m      <span class="discount">${escapeHtml(product.discount || '')}</span>[m
[32m+[m[32m    </div>[m
[32m+[m[32m    <div class="product-info">[m
[32m+[m[32m      <h3>${escapeHtml(product.name)}</h3>[m
[32m+[m[32m      <div>[m
[32m+[m[32m        <span class="stars">★★★★★</span>[m
[32m+[m[32m        <span class="rating">(${escapeHtml(product.rating || '4.8')})</span>[m
[32m+[m[32m      </div>[m
[32m+[m[32m      <div class="price">[m
[32m+[m[32m        <strong>${money(product.price)}</strong>[m
[32m+[m[32m        <span class="old-price">${money(product.old)}</span>[m
[32m+[m[32m      </div>[m
[32m+[m[32m      <small class="stock-note">[m
[32m+[m[32m        ${product.stock > 0 ? `${product.stock}টি স্টকে আছে` : 'স্টক শেষ'}[m
[32m+[m[32m      </small>[m
[32m+[m[32m      <div class="product-actions">[m
[32m+[m[32m        <button class="add-button" data-add="${product.id}" ${product.stock < 1 ? 'disabled' : ''}>[m
[32m+[m[32m          ${product.stock > 0 ? 'ব্যাগে যোগ করুন' : 'স্টক শেষ'}[m
[32m+[m[32m        </button>[m
[32m+[m[32m        <button class="buy-now-button" data-buy-now="${product.id}" ${product.stock < 1 ? 'disabled' : ''}>[m
[32m+[m[32m          ${product.stock > 0 ? 'এখনই কিনুন' : 'স্টক শেষ'}[m
[32m+[m[32m        </button>[m
[32m+[m[32m      </div>[m
[32m+[m[32m    </div>[m
[32m+[m[32m  </article>`;[m
[32m+[m[32m}[m
 function renderNewArrivals() { const selectedIds = new Set((campaignSettings.productIds || []).map(Number)); const selected = products.filter(product => selectedIds.has(Number(product.id))).slice(0, 5); const grid = $('#newArrivalProductGrid'); if (grid) grid.innerHTML = selected.length ? selected.map(productMarkup).join('') : '<p class="empty-state">Admin panel থেকে New arrival products select করুন।</p>'; }[m
 function renderCategoryRows() {[m
   const container = $('#dynamicCategorySections');[m
[36m@@ -869,7 +900,58 @@[m [mfunction renderOrders() {[m
   `;[m
 }[m
 [m
[31m-function showGiftDetails(giftId) { const gift = gifts.find(item => item.id === Number(giftId)); if (!gift) return; const selected = products.filter(product => (gift.productIds || []).map(Number).includes(Number(product.id)) || (gift.giftCategory && productMatchesCategory(product, gift.giftCategory))); const cards = selected.map(product => `<article class="product-card"><div class="product-image"><img src="${escapeHtml(product.image)}" alt="${escapeHtml(product.name)}"><span class="discount">${escapeHtml(product.discount || '')}</span></div><div class="product-info"><h3>${escapeHtml(product.name)}</h3><div class="price"><strong>${money(product.price)}</strong><span class="old-price">${money(product.old)}</span></div><small class="stock-note">${product.stock > 0 ? `${product.stock}টি স্টকে আছে` : 'স্টক শেষ'}</small><div class="product-actions"><button class="add-button" data-add="${product.id}" ${product.stock < 1 ? 'disabled' : ''}>ব্যাগে যোগ করুন</button><button class="buy-now-button add-button" data-buy-now="${product.id}" ${product.stock < 1 ? 'disabled' : ''}>Buy now</button></div></div></article>`).join(''); $('#campaignViewContent').innerHTML = `<div class="campaign-view-header"><p class="eyebrow">FREE GIFT CAMPAIGN</p><h2>${escapeHtml(gift.title)} × ${gift.quantity || 1}</h2><p>এই campaign-এর selected products কিনলে checkout-এ free gift পাবেন।</p></div><div class="campaign-product-grid">${cards || '<p class="empty-state">এই campaign-এ product select করা হয়নি।</p>'}</div>`; openModal('campaignViewModal'); }[m
[32m+[m[32mfunction showGiftDetails(giftId) {[m
[32m+[m[32m  const gift = gifts.find(item => item.id === Number(giftId));[m
[32m+[m[32m  if (!gift) return;[m
[32m+[m
[32m+[m[32m  const selectedIds = new Set((gift.productIds || []).map(Number));[m
[32m+[m
[32m+[m[32m  const selected = products.filter(product =>[m
[32m+[m[32m    selectedIds.has(Number(product.id)) ||[m
[32m+[m[32m    (gift.giftCategory && productMatchesCategory(product, gift.giftCategory))[m
[32m+[m[32m  );[m
[32m+[m
[32m+[m[32m  const unique = new Map([m
[32m+[m[32m    selected.map(product => [Number(product.id), product])[m
[32m+[m[32m  );[m
[32m+[m
[32m+[m[32m  const cards = [...unique.values()].map(product => `[m
[32m+[m[32m    <article class="product-card">[m
[32m+[m[32m      <div class="product-image">[m
[32m+[m[32m        <img src="${escapeHtml(primaryProductImage(product))}" alt="${escapeHtml(product.name)}">[m
[32m+[m[32m        <span class="discount">${escapeHtml(product.discount || '')}</span>[m
[32m+[m[32m      </div>[m
[32m+[m[32m      <div class="product-info">[m
[32m+[m[32m        <h3>${escapeHtml(product.name)}</h3>[m
[32m+[m[32m        <div class="price">[m
[32m+[m[32m          <strong>${money(product.price)}</strong>[m
[32m+[m[32m          <span class="old-price">${money(product.old)}</span>[m
[32m+[m[32m        </div>[m
[32m+[m[32m        <small class="stock-note">[m
[32m+[m[32m          ${product.stock > 0 ? `${product.stock}টি স্টকে আছে` : 'স্টক শেষ'}[m
[32m+[m[32m        </small>[m
[32m+[m[32m        <div class="product-actions">[m
[32m+[m[32m          <button class="add-button" data-add="${product.id}" ${product.stock < 1 ? 'disabled' : ''}>[m
[32m+[m[32m            ${product.stock > 0 ? 'ব্যাগে যোগ করুন' : 'স্টক শেষ'}[m
[32m+[m[32m          </button>[m
[32m+[m[32m        </div>[m
[32m+[m[32m      </div>[m
[32m+[m[32m    </article>[m
[32m+[m[32m  `).join('');[m
[32m+[m
[32m+[m[32m  $('#campaignViewContent').innerHTML = `[m
[32m+[m[32m    <div class="campaign-view-header">[m
[32m+[m[32m      <p class="eyebrow">FREE GIFT CAMPAIGN</p>[m
[32m+[m[32m      <h2>${escapeHtml(gift.title)} × ${gift.quantity || 1}</h2>[m
[32m+[m[32m      <p>এই campaign-এর selected products কিনলে checkout-এ free gift পাবেন।</p>[m
[32m+[m[32m    </div>[m
[32m+[m[32m    <div class="campaign-product-grid">[m
[32m+[m[32m      ${cards || '<p class="empty-state">এই campaign-এ product select করা হয়নি।</p>'}[m
[32m+[m[32m    </div>[m
[32m+[m[32m  `;[m
[32m+[m
[32m+[m[32m  openModal('campaignViewModal');[m
[32m+[m[32m}[m
 function showAdDetails(adId) { const ad = ads.find(item => item.id === Number(adId)); if (!ad) return; const selected = products.filter(product => (ad.productIds || []).map(Number).includes(Number(product.id)) || (ad.targetCategory && productMatchesCategory(product, ad.targetCategory))); const cards = selected.map(productMarkup).join(''); $('#campaignViewContent').innerHTML = `<div class="campaign-view-header"><p class="eyebrow">ZIYANA SHOP CAMPAIGN</p><h2>${escapeHtml(ad.title)}</h2><p>${escapeHtml(ad.text)}</p></div><div class="campaign-product-grid">${cards || '<p class="empty-state">এই campaign-এ product select করা হয়নি।</p>'}</div>`; openModal('campaignViewModal'); }[m
 function showMallProducts() {[m
   const selectedIds = new Set((mallSettings.productIds || []).map(Number));[m
[36m@@ -3189,6 +3271,7 @@[m [masync function handleAdminOrderStatusChange(order, select) {[m
         </div>[m
       `;[m
 [m
[32m+[m[32m      page.hidden = true;[m
       document.body.appendChild(page);[m
     }[m
 [m
[36m@@ -3390,71 +3473,6 @@[m [masync function handleAdminOrderStatusChange(order, select) {[m
 [m
 [m
 [m
[31m-/* PRODUCT DETAIL BUY NOW - DETAILS VIEW ONLY */[m
[31m-(function installProductDetailBuyNow() {[m
[31m-  if (window.__ziyanaBuyNowDetailsOnlyReady) return;[m
[31m-  window.__ziyanaBuyNowDetailsOnlyReady = true;[m
[31m-[m
[31m-  function addBuyNowOnlyInsideDetails() {[m
[31m-    const addButtons = Array.from([m
[31m-      document.querySelectorAll('.modal button, [role="dialog"] button')[m
[31m-    ).filter(button => {[m
[31m-      const text = (button.textContent || '').replace(/\s+/g, ' ').trim();[m
[31m-      return text === 'ব্যাগে যোগ করুন';[m
[31m-    });[m
[31m-[m
[31m-    addButtons.forEach(addButton => {[m
[31m-      if (addButton.parentElement?.querySelector('.product-buy-now-button')) return;[m
[31m-[m
[31m-      const buyNow = document.createElement('button');[m
[31m-      buyNow.type = 'button';[m
[31m-      buyNow.className = 'primary-button product-buy-now-button';[m
[31m-      buyNow.textContent = 'Buy Now';[m
[31m-[m
[31m-      addButton.insertAdjacentElement('afterend', buyNow);[m
[31m-[m
[31m-      buyNow.addEventListener('click', function(event) {[m
[31m-        event.preventDefault();[m
[31m-        event.stopPropagation();[m
[31m-[m
[31m-        addButton.click();[m
[31m-[m
[31m-        setTimeout(() => {[m
[31m-          try {[m
[31m-            if (typeof window.openCheckout === 'function') {[m
[31m-              window.openCheckout();[m
[31m-            }[m
[31m-          } catch (error) {[m
[31m-            console.error('Buy Now checkout failed:', error);[m
[31m-          }[m
[31m-        }, 150);[m
[31m-      });[m
[31m-    });[m
[31m-[m
[31m-    // Remove any accidental Buy Now buttons outside the details/modal view.[m
[31m-    document[m
[31m-      .querySelectorAll('.product-buy-now-button')[m
[31m-      .forEach(button => {[m
[31m-        const insideDetails =[m
[31m-          button.closest('.modal') ||[m
[31m-          button.closest('[role="dialog"]');[m
[31m-[m
[31m-        if (!insideDetails) button.remove();[m
[31m-      });[m
[31m-  }[m
[31m-[m
[31m-  addBuyNowOnlyInsideDetails();[m
[31m-[m
[31m-  const observer = new MutationObserver(() => {[m
[31m-    addBuyNowOnlyInsideDetails();[m
[31m-  });[m
[31m-[m
[31m-  observer.observe(document.body, {[m
[31m-    childList: true,[m
[31m-    subtree: true[m
[31m-  });[m
[31m-})();[m
[31m-[m
 /* PRODUCT DETAILS BUTTON HOVER - FORCE HOME STYLE */[m
 (function forceProductDetailsButtonHover() {[m
   if (window.__ziyanaProductDetailsButtonHoverReady) return;[m
